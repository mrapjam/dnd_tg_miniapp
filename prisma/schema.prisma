// prisma/schema.prisma
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Game {
  id           String   @id @default(cuid())
  code         String   @unique @db.VarChar(6) // 6-символьный код комнаты
  gmId         String   @db.VarChar(32)        // id мастера (telegram user id строкой)
  started      Boolean  @default(false)
  createdAt    DateTime @default(now())
  lastActivity DateTime @default(now())
  expiresAt    DateTime

  // связи
  players  Player[]
  messages Message[]
}

model Player {
  id        String   @id @default(cuid())
  name      String   @db.VarChar(64)
  avatar    String   @default("🛡️") @db.VarChar(8)
  joinedAt  DateTime @default(now())

  // связь с Game
  gameId String
  game   Game   @relation(fields: [gameId], references: [id], onDelete: Cascade)
}

model Message {
  id        String   @id @default(cuid())
  text      String
  createdAt DateTime @default(now())

  // связь с Game
  gameId String
  game   Game   @relation(fields: [gameId], references: [id], onDelete: Cascade)
}
