{
  "name": "dnd-tg-miniapp",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "build:render": "npm install && npx prisma generate",
    "predeploy:render": "npx prisma db push --accept-data-loss"
  },
  "dependencies": {
    "@prisma/client": "^5.17.0",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "telegraf": "^4.16.3"
  },
  "devDependencies": {
    "prisma": "^5.17.0"
  }
}
